# Keep embedded catalog bytes identical in native Windows and POSIX checkouts.
data/available_model_scores.csv text eol=lf
# Provenance fixtures assert byte hashes on every native test platform.
internal/catalog/score/testdata/*.csv text eol=lf
